{
  "name": "@ptensor/typescript",
  "version": "1.0.0",
  "description": "TypeScript bindings for the Ptensor C API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
	"lint": "eslint .",
	"vitest": "vitest run",
	"coverage": "vitest run --coverage"
  },
  "keywords": [
    "tensor",
    "ndarray",
    "numeric",
    "ffi"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "koffi": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
