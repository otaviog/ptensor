{
    "name": "ptensor-view-workspace",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "The ptensor viewer family: the React panel, the tlog server it is fed by, and the two apps that host it (desktop and VS Code).",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "bun run build/ts && bun install && bun run build/view && bun run build/tlog",
        "build/ts": "cd ../ptensor-ts && bun run build",
        "build/view": "bun --filter='@ptensor/tensor-view' run build",
        "build/tlog": "bun --filter='ptensor-tlog' run build",
        "typecheck": "bun --filter='*' run typecheck",
        "test": "bun --filter='*' run test"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}
