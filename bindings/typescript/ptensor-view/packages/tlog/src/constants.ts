/** Default TCP port of the tensor feed (override with PTENSOR_VIEW_PORT). */
export const DEFAULT_PORT = 4449;

/** Default session a tensor lands in when the producer sends no session id. */
export const DEFAULT_SESSION = 'default';
